# Vocaby on Android
Vocaby provides tools that empower individuals learning the English language.

## Terms
**Entry**: A word or a phrase in the dictionary. \
**Entry Group**: A collection of definitions that share the same type/part of speech for a given entry.

## Minimum Viable Product (v1.0.0)
1. Users can look up an entry in the Vocaby dictionary.
    - An entry could have a pronunciation.
    - An entry must have a definition.
    - A definition could have an example.
    - A definition must be part of an entry group.
2. Looked up entries are saved in the search history.
    - This could be cleared.
3. Users could look up an entry.
    - The dictionary could be updated with each subsequent updates
5. Users can save entries (Local).
6. Users can view their saved entries.
    - Saved entries are paginated (15 items per page).
    - Users can delete their saves.
7. Users can edit definitions for existing entries, or create a new entry in the dictionary (Local).
8. Users can create a widget in the home screen to review saved definitions.
    - The widget will refresh periodically, changing the entry to review.
    - The user can refresh the widget manually.
9. Users can turn on notifications to be notified of their saved entries for review.
    - Users can change the notification refresh frequency.
10. Users can view Word of the Day (WoD) on the main screen (Fetched from the Vocaby API).
11. There's an about page 
    - Users can read about our Story and Motivation
    - Users can send feedback
    - Users can view what's upcoming for Vocaby (Fetched from the API)
    - Users can view what the lastest version is for the Android app (Fetched from the API)

**User-defined definitions will always be prioritized over Vocaby definitions.**

## Next Major Update (v2.0.0)
1. User data is synced across platforms when logged in.
2. Users can register/login/logout.
    - On registration, user needs to confirm their email. If the email is not verfied, the account will be deleted in after some predetermined time.
    - Users can reset their password.
    - Users can delete their account.
3. Users could look up an entry using the Vocaby API.
    - Vocaby Dictionary will be maintained to provide broader entry coverage and up-to-date definitions. 

## Extra Features for Future Updates
- Notes
- Multiple custom dictionaries
- Exportable saves
- Exportable dictionary
- Shareable custom dictionary
- Quizzes / Games
- Baskets (Save words in groups)
- Book Scanning

_More to come..._
